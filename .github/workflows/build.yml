name: Build BuyHomes plugin on: push: workflow_dispatch: jobs: build: runs-on: ubuntu-latest steps: - name: Checkout repo uses: actions/checkout@v4 - name: Set up JDK 21 uses: actions/setup-java@v4 with: distribution: temurin java-version: '21' cache: maven - name: Build with Maven run: mvn -B package - name: Upload compiled jar uses: actions/upload-artifact@v4 with: name: BuyHomes-jar path: target/BuyHomes.jar
